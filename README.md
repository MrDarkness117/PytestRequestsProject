# Pytest Requests Project - GigaChat API Testing

Фреймворк по автотестам GigaChat API с использованием PyTest.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` в корне проекта на основе примера:
```env
# Base URL для API (опционально, есть значения по умолчанию)
GIGACHAT_API_BASE_URL=https://gigachat.devices.sberbank.ru/api/v1
GIGACHAT_OAUTH_URL=https://ngw.devices.sberbank.ru:9443/api/v2/oauth
# Токен авторизации
GIGACHAT_BASIC_AUTH_TOKEN=<token>
```

## Запуск тестов

### Запуск всех тестов GigaChat API:
```bash
pytest -v -m gigachat
```

## Генерация Allure отчетов

Тесты уже размечены аннотациями Allure (`feature`, `story`, `severity`, шаги и вложения).

1. **Запуск тестов с генерацией Allure-результатов:**
```bash
pytest -m gigachat --alluredir=allure-results
```

2. **Быстрый просмотр отчета:**
```bash
allure serve allure-results
```

3. **Генерация статического HTML отчета:**
```bash
allure generate allure-results -o allure-report --clean
allure open allure-report
```

### Фильтрация тестов в Allure

В Allure вы увидите:
- **Feature:** `GigaChat API`
- **Story:** `chat/completions`
- **Severity:** `CRITICAL`, `NORMAL`, `MINOR` — для разных сценариев

Это позволяет удобно фильтровать тесты и анализировать критичные сценарии.

## Интеграция с Jenkins CI/CD

Проект готов для автоматизации в Jenkins с публикацией Allure отчетов.

> 📖 **Подробная инструкция:** См. [JENKINS_SETUP.md](JENKINS_SETUP.md) для детальной настройки Jenkins.

### Предварительные требования

1. **Установите Allure Plugin в Jenkins:**
   - Перейдите в `Manage Jenkins` → `Manage Plugins`
   - Найдите и установите плагин `Allure Plugin`
   - Перезапустите Jenkins

2. **Настройте Allure Commandline в Jenkins:**
   - Перейдите в `Manage Jenkins` → `Global Tool Configuration`
   - Найдите секцию `Allure Commandline`
   - Добавьте установку Allure (или укажите путь к существующей установке)
   - Сохраните настройки

3. **Настройте переменные окружения в Jenkins:**
   
   В Jenkins Job настройте переменные окружения (можно через `Credentials` или `Environment variables`):
   - `GIGACHAT_BASIC_AUTH_TOKEN` — токен авторизации
   - `GIGACHAT_API_BASE_URL` (опционально)
   - `GIGACHAT_OAUTH_URL` (опционально)
   - `GIGACHAT_SSL_CERT` (опционально, для SSL сертификата)

   **Рекомендуемый способ:** Используйте Jenkins Credentials (Secret text) для хранения токена.

### Настройка Jenkins Job

#### Вариант 1: Использование Jenkinsfile (Pipeline)

1. Создайте новый Pipeline Job в Jenkins
2. В настройках Pipeline выберите:
   - **Definition:** `Pipeline script from SCM`
   - **SCM:** `Git`
   - **Repository URL:** URL вашего репозитория
   - **Script Path:** `Jenkinsfile` (по умолчанию)

3. Jenkinsfile уже настроен и включает:
   - Установку зависимостей Python
   - Запуск тестов с генерацией Allure результатов
   - Автоматическую публикацию Allure отчетов

#### Вариант 2: Freestyle Job

1. Создайте новый Freestyle Job
2. В секции **Build Steps** добавьте:
   ```bash
   # Установка зависимостей
   pip install -r requirements.txt
   
   # Запуск тестов
   pytest -v -m gigachat --alluredir=allure-results --tb=short
   ```

3. В секции **Post-build Actions** добавьте:
   - **Publish Allure Report**
   - **Results path:** `allure-results`
   - Сохраните

### Просмотр отчетов в Jenkins

После выполнения сборки:
- В интерфейсе Jenkins Job появится ссылка **Allure Report**
- Отчет будет доступен для каждой сборки
- Можно сравнивать отчеты между сборками
- Все аннотации Allure (feature, story, severity, шаги) будут отображаться в отчете

### Запуск тестов в Docker

Проект включает Dockerfile и docker-compose.yml для запуска тестов в контейнере.

#### Вариант 1: Docker Compose (рекомендуется)

1. Создайте файл `.env` в корне проекта с переменными окружения:
   ```env
   GIGACHAT_BASIC_AUTH_TOKEN=your_token_here
   ```

2. Запустите тесты:
   ```bash
   docker-compose up --build
   ```

3. Результаты Allure будут в папке `allure-results/` на хосте

#### Вариант 2: Docker напрямую

1. Соберите образ:
   ```bash
   docker build -t gigachat-tests .
   ```

2. Запустите контейнер с переменными окружения:
   ```bash
   docker run --rm \
     -e GIGACHAT_BASIC_AUTH_TOKEN=your_token_here \
     -v $(pwd)/allure-results:/app/allure-results \
     gigachat-tests
   ```

#### SSL Сертификаты

Проект поддерживает использование SSL сертификатов для безопасного подключения к GigaChat API.

**Без сертификата (по умолчанию):**
- Проверка SSL отключена (`verify=False`)
- Работает "из коробки", но менее безопасно

**С сертификатом (рекомендуется для продакшена):**

1. Получите SSL сертификат от Сбербанка (`.pem` файл)
2. Создайте директорию и положите сертификат:
   ```bash
   mkdir -p certs
   cp your_cert.pem certs/sber_ca_bundle.pem
   ```

3. Установите переменную окружения:
   ```bash
   # В .env файле или через docker-compose
   GIGACHAT_SSL_CERT=/certs/sber_ca_bundle.pem
   ```

4. При запуске через Docker Compose сертификат автоматически будет смонтирован и использован

**Локальный запуск (без Docker):**
```bash
export GIGACHAT_SSL_CERT=/path/to/sber_ca_bundle.pem
pytest -v -m gigachat
```

Если сертификат указан и файл существует, он будет использован автоматически. Иначе проверка SSL будет отключена.

## Структура проекта

```
.
├── tests/
│   ├── __init__.py          # Базовые функции для работы с API
│   ├── conftest.py          # Фикстуры и настройки pytest
│   ├── schemas.py           # JSON схемы для валидации ответов
│   └── test_gigachat_api.py # Тесты для GigaChat API
├── requirements.txt         # Зависимости проекта
├── pytest.ini              # Конфигурация pytest
├── Jenkinsfile              # Jenkins Pipeline для CI/CD
├── Dockerfile               # Docker образ для запуска тестов
├── docker-compose.yml       # Docker Compose конфигурация
└── .env                    # Переменные окружения (создать самостоятельно)
```

## Тесты

Тесты для GigaChat API находятся в файле `tests/test_gigachat_api.py` и включают:

- Базовый тест отправки сообщения
- Тест с системным промптом
- Тест диалога с несколькими сообщениями
- Тесты с различными параметрами (temperature, max_tokens)
- Тесты обработки ошибок
- Тесты моделей
- Детальная проверка структуры ответа

Все тесты используют валидацию JSON схем для проверки структуры ответов API.


